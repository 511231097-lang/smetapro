import { Box, Text, Title } from '@mantine/core';

export const ReferencesPage = () => {
  return (
    <Box p={20}>
      <Title order={2}>Справочники</Title>
      <Text c="dimmed" mt={6}>
        Информация по справочникам появится здесь.
      </Text>
    </Box>
  );
};

export default ReferencesPage;
